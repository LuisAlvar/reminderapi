name: ${BuildId}

# CI triggers 
trigger: 
  batch: true 
  branches: 
    include: 
      - main 

pool: 
  vmImage: 'ubuntu-latest'

stages:
  - stage: Azure dev pipeline 
    displayName: Azure dev pipeline